#include <bits/stdc++.h>
using namespace std;

string T2K(){
    string result;
    return result;
}

string K2T(){
    string result;
    return result;
}

int main() {
    string str;
    vector<string> input;
    while (getline(str)) {
        istringstream iss(str);
        string item;
        while (iss >> item) {
            input.push_back(item);
        }
    }
    return 0;
}